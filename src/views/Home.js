import { data } from "../data/dataset";
import { filterGenders, sortFilms, computeStats } from "../Lib/dataFunctions";

export function Home(props) {
  const viewEl = document.createElement("div");
  viewEl.textContent = "";
  return viewEl
}